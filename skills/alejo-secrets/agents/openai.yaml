interface:
  display_name: "Alejo Secrets"
  short_description: "Auto-recover safe provider secrets into Doppler"
  default_prompt: "Use $alejo-secrets to inventory required secrets, dedupe them, automatically recover anything safe from existing authenticated providers or generated app values into Doppler, then give me a short official-link setup guide only for values that truly require human action."
