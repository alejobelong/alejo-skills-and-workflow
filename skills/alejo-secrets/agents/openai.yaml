interface:
  display_name: "Alejo Secrets"
  short_description: "Research and dedupe Doppler setup"
  default_prompt: "Use $alejo-secrets to research where to get each required provider key, dedupe reused secrets across slices/issues, then guide me through creating each Doppler secret with pbpaste commands that clear the clipboard."
