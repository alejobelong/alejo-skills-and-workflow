interface:
  display_name: "Alejo Run"
  short_description: "Run issues to production-grade done"
  default_prompt: "Use $alejo-run to preflight ready Linear implementation issues, recover safe missing Doppler secrets via Alejo Secrets, launch the documented Symphony flow after approval, and keep iterating until real-surface verification plus a fresh Alejo Review subagent both pass cleanly with no mocks, fakes, stubs, placeholders, skipped tests, or half-working paths."
