interface:
  display_name: "Alejo Run"
  short_description: "Approve and hand ready issues to Symphony"
  default_prompt: "Use $alejo-run to preflight ready Linear implementation issues, including preconditions and readiness fields, ask for approval, and move approved issues into the Symphony execution lane."
