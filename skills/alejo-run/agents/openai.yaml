interface:
  display_name: "Alejo Run"
  short_description: "Preflight and hand ready issues to Symphony"
  default_prompt: "Use $alejo-run to preflight ready Linear implementation issues, recover safe missing Doppler secrets via Alejo Secrets, ask for approval, and move approved issues into the Symphony execution lane."
